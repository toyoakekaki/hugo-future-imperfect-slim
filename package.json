{
  "name": "my-hugo-project",
  "scripts": {
    "dev-test": "contentful-hugo --preview && hugo server",
    "dev": "contentful-hugo --preview && python -m app -f && hugo server",
    "build": "contentful-hugo && hugo --minify"
  },
  "dependencies": {
    "contentful-hugo": "^1.15.0"
  }
}
